export enum IsFetchingEnum {
  unloaded = "unloaded",
  loaded = "loaded",
  loading = "loading",
  error = "error",
}

export enum InputFocusEnum {
  receive = "receive",
  send = "send",
}

export enum TabEnum {
  swap = "swap",
  deposit = "deposit",
  wallet = "wallet",
}
